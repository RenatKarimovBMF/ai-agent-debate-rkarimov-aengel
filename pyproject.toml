[project]
name = "ai-agent-debate"
version = "0.1.0"
description = "Exercise 02 — mediated AI debate: The Godfather vs The Shawshank Redemption"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Renat Karimov" },
    { name = "Alon Engel" },
]
dependencies = [
    "google-genai>=1.0.0",
    "anthropic>=0.40.0",
    "httpx>=0.27.0",
    "pydantic>=2.9.0",
    "pydantic-settings>=2.6.0",
    "python-dotenv>=1.0.0",
    "structlog>=24.4.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "pytest-cov>=6.0.0",
    "ruff>=0.8.0",
]

[project.scripts]
debate = "debate.main:main"
debate-gui = "debate.gui:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
only-include = ["src/debate", "src/sdk"]

[tool.hatch.build.targets.wheel.force-include]
"src/debate" = "debate"
"src/sdk" = "sdk"

[tool.pytest.ini_options]
testpaths = ["tests/unit", "tests/integration"]
pythonpath = ["src"]

[tool.coverage.run]
source = ["src/debate", "src/sdk"]
omit = [
    "*/debate/test_gemini.py",
    "*/debate/gui/*",
    "*/debate/main.py",
    "*/debate/process_orchestrator.py",
    "*/debate/single_orchestrator.py",
    "*/debate/orchestrator/child_worker.py",
    "*/debate/orchestrator/parent_worker.py",
    "*/debate/orchestrator/ping_round.py",
    "*/debate/orchestrator/process_pool.py",
    "*/debate/orchestrator/supervisor.py",
    "*/debate/orchestrator/supervisor_watchdog.py",
    "*/debate/legacy/session_loop.py",
    "*/debate/legacy/debate_orchestrator.py",
]

[tool.coverage.report]
fail_under = 85
show_missing = true
skip_empty = true

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
